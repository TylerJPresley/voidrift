[tool.pytest.ini_options]
testpaths = ["cli/tests", "mcp-context-server/tests", "worker-cli/tests"]
pythonpath = ["cli/tests", "mcp-context-server/tests", "worker-cli/tests"]
addopts = "-v --tb=short"
